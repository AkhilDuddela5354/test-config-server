

spring:
  datasource:
    url: jdbc:h2:mem:telemetry
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always

server:
  port: 8080

asset-service:
  url: ${ASSET_SERVICE_URL:http://localhost:8081}
